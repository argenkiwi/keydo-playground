{
  "name": "keydo-playground",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ./keydo-wasm --target web --out-dir ../src/wasm",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
